# ==============================================================================
# FranklinWH Battery Automation - Python Dependencies v4.6.0
# ==============================================================================
# v4.6 added no new dependencies — config_store.py and rate_config.py use only
# the standard library (sqlite3, base64, json).

# Core Franklin WH API
franklinwh>=0.1.0

# Configuration management
python-dotenv>=1.0.0

# HTTP requests (for pricing API, weather, PVOutput)
requests>=2.28.0

# Chart generation
pandas>=2.0.0
matplotlib>=3.7.0

# Internal task scheduler (for Docker)
schedule>=1.2.0

# ==============================================================================
# Modbus & Async Dependencies (added in v3.5.0)
# ==============================================================================

# Modbus TCP integration for local data collection
pymodbus>=3.5.0

# Async HTTP client for telemetry (optional)
aiohttp>=3.8.0

# Additional async support
asyncio-mqtt>=0.13.0

# ==============================================================================
# SolarEdge /services/ energy API auth (added for portal collector revival)
# ==============================================================================
# The newer /services/.../by-inverter endpoint (per-optimizer energy + color
# health) authenticates via an AWS Cognito SRP token. pycognito handles the
# SRP handshake. Pulls boto3/botocore + pyjwt[crypto] transitively.
pycognito>=2024.5.1
