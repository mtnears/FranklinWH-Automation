# Scripts
   
   All automation scripts. Replace placeholder credentials with your own before use.
```
   - Commit

### **Then Upload Your Files:**

1. Click into the `docs` folder
2. **"Add file" → "Upload files"**
3. Drag both PDFs
4. Commit

Same for `scripts` folder - upload all your `.py` files.

---

## **Alternative: Upload Everything at Once**

If you're uploading via web interface:

1. **"Add file" → "Upload files"**
2. **Create a local folder structure first:**
```
   FranklinWH-Automation/
   ├── docs/
   │   ├── FRANKLIN_BATTERY_AUTOMATION_GUIDE.pdf
   │   └── FRANKLIN_BATTERY_SCRIPTS.pdf
   └── scripts/
       ├── morning_solar_intelligence.py
       ├── midday_charge_check.py
       └── ... (all other scripts)
